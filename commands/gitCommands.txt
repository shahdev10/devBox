git add .  OR  git add <filename>

git commit -a -m "Some message"

git push -u origin master

General commands
==============
git status
git log
git log -2 //last 2 commit history

Add Repository
================
which git

***go to git folder***

git clone (Paste link)

Add Branch / Commit for Pull Request
====================================
git checkout -b new_branch
git add <file>
git commit -a -m "some message"
git push -u origin new_branch

then go to website and create pull request
